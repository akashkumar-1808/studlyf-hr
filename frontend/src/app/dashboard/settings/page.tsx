import SettingsPage from "@/components/dashboard/SettingsPage";

export const metadata = {
  title: "Settings | Studlyf HR",
  description: "Manage system settings",
};

export default function Page() {
  return <SettingsPage />;
}
